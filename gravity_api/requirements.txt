# Gravity NIL Intelligence API (gravity_api/) — runtime deps.
# Install with: pip install -r requirements.txt
# Keep this stack light; scrapers (gravity-scrapers) and torch-based scoring (gravity-ml)
# run in separate repositories.
fastapi>=0.115.0
uvicorn[standard]>=0.31.0
asyncpg>=0.30.0
anthropic>=0.39.0
numpy>=1.26.0
pandas>=2.2.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.9.0
PyJWT>=2.8.0
bcrypt>=4.1.0
pytest>=7.4.0
