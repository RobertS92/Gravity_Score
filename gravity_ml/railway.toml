# Railway config for gravity-ml inference service.
# Settings → Root Directory: gravity_ml (or repo root with dockerfilePath below)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health/ready"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
